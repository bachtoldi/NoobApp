<UserControl x:Class="NoobApp.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoobApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <ListBox HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="0">
            <Button Width="250" Height="40" Command="{Binding AddPurchaseCmd}" Content="Artikel konsumieren" />
            <Button Width="250" Height="40" Command="{Binding AttendanceCmd}" Content="Anwesenheit definieren" />
            <Button Width="250" Height="40" Command="{Binding ViewPurchasesCmd}" Content="Aktuelle Abrechnung" />
        </ListBox>
        
    </Grid>
</UserControl>
